# PlaylistVault Browser Handoff

Load this folder unpacked from Chrome/Edge extensions with Developer mode enabled.
The extension sends only the current HTTPS URL to the local `playlistvault://`
protocol. It has no cookie, browsing-history, or external network permissions.
